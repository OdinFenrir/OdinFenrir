name: Generate Snake

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: OdinFenrir
          outputs: |
            dist/github-snake.svg?palette=github-dark&color_snake=purple&color_dots=#0f1f16,#123526,#145f2f,#1c8d44,#26e26d
            dist/github-snake-dark.svg?palette=github-dark&color_snake=purple&color_dots=#0f1f16,#123526,#145f2f,#1c8d44,#26e26d

      - name: Publish snake assets
        uses: crazy-max/ghaction-github-pages@v5
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
